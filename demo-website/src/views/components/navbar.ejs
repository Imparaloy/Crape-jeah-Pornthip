<!-- views/components/navbar.ejs -->
<nav class="bg-[#C24025] text-[#FDECC8] shadow-sm">
  <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
    <a href="/" class="flex items-center space-x-3">
      <span class="text-xl font-semibold tracking-wide">J' Pornthip</span>
    </a>
    <div class="flex-1 flex justify-center">
      <div class="flex items-center space-x-10">
        <a href="/" class="nav-link text-[#FDECC8] hover:text-white transition-colors">Home</a>
        <a href="/order" class="nav-link text-[#FDECC8] hover:text-white transition-colors">Order</a>
        <a href="/status" class="nav-link text-[#FDECC8] hover:text-white transition-colors">Status</a>
      </div>
    </div>
    <div class="flex items-center space-x-4">
      <a id="nav-login" href="/login" class="hidden bg-[#FDECC8] text-[#C24025] px-5 py-2 rounded-full font-medium hover:bg-white transition-colors">login / sign up</a>
      <div id="nav-user-menu" class="hidden items-center space-x-3 text-[#FDECC8]">
        <span>Hi, <span id="nav-username" class="font-semibold"></span></span>
        <a id="nav-profile" href="/profile" class="bg-[#FDECC8] text-[#C24025] px-5 py-2 rounded-full font-medium hover:bg-white transition-colors">Profile</a>
        <button id="nav-logout" class="bg-[#FDECC8] text-[#C24025] px-5 py-2 rounded-full font-medium hover:bg-white transition-colors">Logout</button>
      </div>
    </div>
  </div>
</nav>
<script>
  (function highlightActiveNav() {
    const current = window.location.pathname;
    const links = document.querySelectorAll('.nav-link');
    links.forEach((link) => {
      const href = link.getAttribute('href');
      const isActive = href === '/' ? current === '/' : current.startsWith(href);
      if (isActive) {
        link.classList.add('font-semibold', 'underline', 'underline-offset-4');
      } else {
        link.classList.remove('font-semibold', 'underline', 'underline-offset-4');
      }
    });
  })();
</script>
<script src="/js/auth.js"></script>
