<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order | J' Pornthip</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <%- include('components/navbar') %>
  <main class="container mx-auto px-4 py-10">
    <section class="mb-10">
      <h1 class="text-3xl font-bold text-red-700 mb-6">เลือกเมนูที่คุณชอบ</h1>
      <div class="mb-6">
        <a href="/customize" class="inline-flex items-center bg-red-100 text-red-700 px-5 py-2 rounded-full hover:bg-red-200 transition-colors">สร้างเครปของคุณเอง</a>
      </div>
      <% if (menus && menus.length) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% menus.forEach(menu => { %>
            <div class="bg-white rounded-xl shadow p-6 flex flex-col justify-between">
              <div>
                <h2 class="text-xl font-semibold text-gray-800"><%= menu.name %></h2>
                <p class="text-gray-500 mt-2"><%= menu.description || 'อร่อยทุกคำ' %></p>
                <p class="text-red-700 font-bold text-lg mt-4">฿<%= menu.price %></p>
              </div>
              <button data-menu-id="<%= menu.id %>" class="mt-6 bg-red-700 text-white py-2 rounded-lg hover:bg-red-800 transition-colors add-to-cart">เพิ่มลงตะกร้า</button>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="text-gray-500">ไม่มีเมนูในขณะนี้</p>
      <% } %>
    </section>
  </main>
  <script src="/js/order.js"></script>
</body>
</html>
