<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Status | J' Pornthip</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#fdf4ee] min-h-screen">
  <%- include('components/navbar') %>
  <main class="container mx-auto px-4 py-10">
    <section class="mb-10">
      <h1 class="text-3xl font-bold text-red-700 mb-6">สถานะคำสั่งซื้อ</h1>
      <div id="status-alert" class="hidden bg-yellow-100 text-yellow-900 px-4 py-3 rounded mb-6"></div>
      <div id="cart-container" class="bg-white rounded-xl shadow p-6">
        <div id="cart-loading" class="text-gray-500">กำลังโหลด...</div>
        <table id="cart-table" class="hidden min-w-full leading-normal">
          <thead>
            <tr>
              <th class="px-5 py-3 border-b-2 border-gray-200 bg-gray-100 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">เมนู</th>
              <th class="px-5 py-3 border-b-2 border-gray-200 bg-gray-100 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">จำนวน</th>
              <th class="px-5 py-3 border-b-2 border-gray-200 bg-gray-100 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">หมายเหตุ</th>
            </tr>
          </thead>
          <tbody id="cart-rows"></tbody>
        </table>
        <div id="cart-empty" class="hidden text-gray-500">ยังไม่มีรายการในตะกร้า</div>
      </div>
    </section>
  </main>
  <script src="/js/status.js"></script>
</body>
</html>